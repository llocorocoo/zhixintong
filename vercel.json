{
  "buildCommand": "cp src/network.mock.ts src/network.ts && pnpm build:web",
  "outputDirectory": "dist-web",
  "installCommand": "pnpm install",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
